1.安装Mysql
# yum list mysql-server
# yum -y install mysql-server

2.启动Mysql服务
# service mysqld start

3.链接
# mysql
mysql> 
ctr + c，退出

4.设置Mysql开机启动
# chkconfig mysqld on

5.修改密码并设置远程访问
# mysql
mysql> use mysql;
mysql> update user set password=password('root') where user='root'; --修改root登录msql的密码
mysql> flush privileges;
mysql> grant all privileges on *.* to 'root'@'%' identified by 'root' with grant option;
mysql> -- 'root'@'%' identified by 'root'，允许任何IP地址（%）的电脑用帐户（root）和密码（root）来访问MySQL Server
mysql> -- 'root'@'192.168.0.1'，%号可以用IP地址替换，意思是仅允许指定的IP地址访问MySQL Server

6.解决Mysql乱码
# vim /etc/my.cnf
在[mysqld]下面添加
default-character-set=utf8

7.重启mysql服务
# service mysqld restart

8.安装phpMyAdmin
1).安装php5
yum -y update
yum -y install epel-release
wget https://dl.fedoraproject.org/pub/epel/epel-release-latest-6.noarch.rpm
wget https://centos6.iuscommunity.org/ius-release.rpm
rpm -Uvh ius-release*.rpm
yum -y update
yum -y install php55u php55u-opcache php55u-xml php55u-mcrypt php55u-gd php55u-devel php55u-mysql php55u-intl php55u-mbstring php55u-bcmath

2).更新apache
# rpm -qa httpd  --查看是否安装了apache
httpd-2.2.15-55.el6.centos.2.x86_64
# yum update httpd  --更新apache

3).安装phpMyAdmin
https://www.phpmyadmin.net/downloads/
# tar -zxvf /..目录/phpMyAdmin-4.6.5.2-all-languages.tar.gz -C /var/www/html
# cd /var/www/html
# mv phpMyAdmin-4.6.5.2-all-languages phpMyAdmin  --重命名文件夹
# cd phpMyAdmin
# cp config.sample.inc.php config.inc.php
# service httpd restart
